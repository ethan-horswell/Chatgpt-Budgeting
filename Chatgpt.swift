 
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Expense Tracker App")
                .font(.largeTitle)
                .padding()
            
            Text("Track your expenses easily.")
                .font(.subheadline)
        }
    }
}

#Preview {
    ContentView()
}
